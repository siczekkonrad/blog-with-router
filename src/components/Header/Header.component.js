import React from 'react'
import NavMenu from '../NavMenu/NavMenu.component';

const Header = () => {
    return (
        <header>
            <NavMenu />
        </header>
    )
}

export default Header